import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.malhotra.newsecurityy.mobile',
  appName: 'Malhotra Guvenlik Mobil',
  webDir: 'build'
};

export default config;
